syntax = "proto3";

package user;

service UserService {
  rpc UserMessage (User) returns (Reply) {}
}

message User{
  string username = 1;
  string password = 2;
}

message Reply {
  string token = 1;
}